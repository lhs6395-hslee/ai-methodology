# HARNESS — SDD 인터랙티브 sync 계약 (포터블)

> spec↔code 생애주기를 **사람-개입**으로 오케스트레이션하는 플로우 계약. 이 규칙표가 SSOT — 실행기(에이전트별)는 이 표를 해석한다. 탐지는 기존 게이트 재사용(판정 로직 신규 0). 결정은 항상 사람(작성=LLM, 승인=사람). 설계 근거: `docs/design/2026-06-30-harness-design.md`.

## 규칙표 {trigger, detect, ask, act}

| 규칙 | Trigger | Detect (게이트) | Ask (사람) | Act |
|---|---|---|---|---|
| **R1 spec→code** | spec 생성/변경 | `check-fr-coverage`(테스트 없는 FR ≈ 미구현 + 접두어↔클래스(SPEC-012, hard)·번호 무결성(SPEC-014)·TEST 인프라 격리(SPEC-015)·**Change Log가 선언한 FR의 실재**(SPEC-037 — `FR-018 신규`라고 적었으면 본문이 있어야 한다, `changeLogFrRefPolicy`)·**지목 구현체 참조**(SPEC-046 — FR이 백틱으로 `name()`·`name.ext`를 지목했으면 그 이름이 실행 경로에서 **참조**돼야 한다: 정의뿐이면 고아 구현이고, 표면이 같은 일을 따로 구현했다는 뜻일 수 있다. 커버 파일이 그 이름을 모르면 그 테스트는 FR의 주장이 아니라 현재 구현의 형태를 단언하고 있을 수 있다. `implReferencePolicy`)·**`@covers` 양방향 결속**(SPEC-039 — 태그가 가리키는 FR과 FR이 인정하는 `[검증]` 목록이 서로를 아는가, `coversBacklinkPolicy`)) | "이 FR들 코드 생성/업데이트?" | TDD(RED→GREEN) → 재검증 |
| **R2 code→spec** | 코드 변경·spec 무변경 | `check-converge-drift`·`check-orphan-surfaces`·`check-spec-sync`(range — Draft 소유 차단·unowned 정책 포함) | "기존 spec 개정 / 새 spec / 의도적 무시?" | `/converge`→intent→`/specify`(update·new)→`/analyze`→bless |
| **R3 dedup+입도+완전성+일관성** | spec 생성/변경 직후 | `check-ownership`(+`entityRegistry`·Files 카테고리 금지·Entity 관계(SPEC-017 — 실재 hard·순환 advisory)·Capability 귀속(SPEC-024 — entity 기준 스펙 경계))·`check-spec-cohesion`·`check-spec-completeness`(SC·인수조건 + 수명주기 기록 + Change Log 근거 + 문법 규범: Module 존재·단일성·SHALL·Dedup 참조 실재 + 오브젝트 스토리지 결정(SPEC-016 advisory))·`check-spec-consistency`(+FR 키 앵커 — bold↔소유·참조 키 대조, `frKeyAnchorPolicy`, SPEC-023) | "중복 통합 / 과대 spec 분할 / SC·인수조건·리뷰 기록·근거·문법 보강 / 근거 없는 키 정렬?" | 통합·분할·보강 → 재검증 |
| **R2′ code→deployed-runtime** | 배포 전(preflight) | `check-schema-drift`(코드 기대 스키마 ↔ 배포 DB 실측 diff — opt-in `schemaDriftManifest`, SPEC-022) | "배포 전 마이그레이션 적용 / baseline?" | migrate-on-deploy 또는 배포전 스키마 preflight |
| **R4 상시 sync** | push·주기·요청 | 위 일괄(`sdd-sync.mjs`) | drift → 해당 규칙 라우팅 | (R1/R2/R3의 act) |
| **R5 test 실행** | push(pre-push)·CI·완료 주장 시 | `check-test-run`(`runTestsPolicy` off=no-op·advisory·hard — `commands.test` 실행·exit 0 요구, SPEC-021) | "스위트 red — 완료 주장 전 green으로?" | 실패 테스트 수정 → 재실행 |
| **R8 실행 증거** | spec 저술·리뷰·CI | `check-evidence`(`[검증]`→실행 가능한 증거 경로 강제, SC 실행동사·UI 대상 등급 판정 — `executionEvidencePolicy` off=no-op·advisory·hard, SPEC-031) | "이 주장을 무엇으로 재현하나 — 경로가 있나?" | 증거 자산 작성 후 `[검증: <경로>]`로 지목, 없으면 자기신고 등급으로 정직하게 표기 |
| **R9 라이브 대조** | 배포 전·주기·가오픈 점검 | `check-live-reality` — **두 축**: ①**등록**(선언된 배포 산출물에 담당 검사가 있는가 — `deployArtifactMarkers`·`covers`, 순수 선언 대조라 **오프라인·위임 무관하게 판정**. 실측: 새 산출물 8개 결함을 배포로 하나씩 발견했고 R9 틀은 있었지만 검사 6건에 하나도 없었다) ②**실행**(저장소 밖 진실 — IaC state 미등재 선언, 라이브↔매니페스트 해시, 무소유 자원. 주입 어댑터 `liveRealityChecks`, 자격증명 없으면 **skipped(reason)**, SPEC-032) | "선언과 실물이 어긋났다 — 어느 쪽이 정본인가?" | 라이브가 최신이면 저장소를 먼저 맞춘 뒤(drift 흡수) 변경을 얹고, 결과를 인프라 스펙 Change Log에 기록 |
| **R10 동의어·형태 변이** | spec 생성/변경·주기 | `check-synonym`(dedup이 **키 문자열 유일성**만 보는 사각 — entity 역할 한정 3층: ①형태 변이(정규화 후 충돌)·②선언 동의어(`synonymRegistry`) = **결정적, `synonymPolicy` 강도대로 차단** / ③유사 후보(`entitySimilarityCommand` 주입) = **어떤 강도에서도 비차단**. 후보 목록 신선도(`# entity-set` 헤더 ↔ 현재 집합)도 비차단 advisory, SPEC-033) | "같은 실체면 정본 하나로 통일 / 다르면 기각 사유는? / 후보 목록이 낡았으면 재생성?" | 정본 통합(스펙 편집) 또는 `synonymReviewLedger`에 기각 사유 — 둘 다 **사유 필수**라 config 리뷰를 거친다 |
| **R11 SC·NFR 검증 회계** | spec 저술·리뷰·CI | `check-sc-coverage`(성공기준·비기능 목표가 검증 바인딩을 갖는가 — `[검증: 경로]` ∨ `evidenceManifest`의 증거·유예. FR만 회계하던 사각, `scCoveragePolicy` off=no-op·advisory·hard, SPEC-034) | "이 성능·보안 목표를 무엇이 검증하나 / 지금 못 돌리면 증거·사유는?" | 검증 자산 작성 후 `[검증: <경로>]`, 라이브 필요 시 `evidenceManifest`에 실행 로그·스냅샷 경로와 사유 |
| **R12 훅 배선 실재** | 채택·update·주기 | `check-hooks-installed`(선언된 훅이 `.git/hooks`에 설치·실행 가능·킷 훅인가 — 게이트 파일이 있어도 훅이 없으면 **한 번도 발동하지 않는다**. `hooksInstalledPolicy` off·advisory(기본)·hard, SPEC-036) | "강제가 실제로 걸려 있나 — 미설치·실행권한·남의 훅 점유?" | `sh scripts/sdd-hooks-install.sh`로 배선(설치기가 `hooks.list` 전체를 깔고 자기검증) · **신선도**: 설치본이 `hooks.list`에 선언된 원본과 내용이 같은가(`stale`은 미설치와 동급 — 훅이 있어도 낡으면 그 게이트는 발동하지 않는다). 원본 미선언은 판정하지 않고, 선언됐는데 못 읽으면 `source-unreadable`로 **확인 못 함**을 계상한다(통과 아님) |
| **R13 구현 중복(선언 아닌 구현)** | spec/코드 변경·수시 | `check-duplicate-logic`(dedup의 거울 밖 — dedup은 **선언 단위**(같은 파일·키를 두 스펙이 주장), 이 규칙은 **구현 단위**(같은 규칙을 두 코드가 실현). ①동일 규칙 리터럴 2곳 이상 = 결정적, 강도대로 차단 / ②구조 중복(같은 본문·다른 이름)은 **어댑터 위임**(`duplicateLogicCommand`)이고 **어떤 강도에서도 비차단**. `duplicateLogicPolicy` off·advisory(기본)·hard, SPEC-038) | "이 둘이 같은 규칙인가 — 하나로 합칠까 / 정말 무관한 중복인가?" | 정본 하나로 통합하고 나머지는 호출로 대체, 또는 `duplicateLogicAllow`에 **사유와 함께** 등록(면제는 부채로 상시 표면화) |
| **R6 정책 래칫** | config 변경·push·CI | `check-policy-ratchet`(강제 강도의 단조성 — base 대비 **정책 knob 하향**과 **수치 임계 상향**(`maxFRsPerSpec` 등: 자를 늘려 재는 것도 완화다) 차단, `policyRatchetPolicy` off=no-op·advisory·hard, 예외는 `policyRatchetExceptions`, SPEC-027). **자기포함:** `policyRatchetPolicy` 자신도 래칫 대상이고 자기 강도는 base 시점과 현재 중 강한 쪽으로 판정된다 — 이 knob을 내려 판정을 끄는 한 줄 자폭이 불가하며, 정당한 롤백은 예외 선언(부채 표면화)으로만 가능 | "강도를 왜 낮췄나 / 캡을 왜 올렸나 — 위반은 **스펙 편집·분할·병합**으로 해소 / 진짜 재조정이면 예외 선언?" | knob 복원 또는 예외 선언 → 위반은 스펙 편집으로 소진 |
| **R14 검증 실행(존재 아닌 실행)** | 커밋·push·CI·배포 | `check-verification-executed`(선언된 `[검증: <경로>]` 자산이 **실제로 돌았는가** — 판정 입력은 실행 원장 `verificationRunLedger`(JSONL)이고 러너·CI 스테이지·에이전트가 `--record <asset> <outcome> [사유]`로 남긴다. `outcome`은 SPEC-040의 다섯 종류를 그대로 쓴다. 기록 없음=**침묵**·사유 없는 포기=**깨진 기록**만 차단하고 사유 있는 미실행은 부채로 표면화. 원장 미선언이면 inert. `verificationRunPolicy` off·advisory(기본)·hard, SPEC-041) | "이 검증이 정말 돌았나 — 아니면 조용히 사라졌나?" | 러너·스테이지가 자기 결과를 기록하게 배선. 못 돌았으면 **사유와 함께** 남긴다(포기는 허용, 침묵은 금지) |
| **R15 소개 문서 동기** | 규칙표·정책 변경·커밋·주기 | `check-intro-doc`(방법론을 **설명하는 문서**가 도구를 따라잡는가 — ①규칙표의 규칙 ID가 소개 문서에 다 있는가(신설 규칙 누락) ②문서가 `data-sdd-count`로 기계 검산에 올린 숫자가 실제와 같은가(낡은 숫자) ③규칙표를 고친 changeset에 소개 문서가 함께 있는가(동반 갱신). `introDocs` 미선언이면 inert, `introDocPolicy` off·advisory(기본)·hard, SPEC-045). **왜 게이트인가:** 새로 배우는 사람은 코드가 아니라 이 문서로 방법론을 만난다 — 문서가 낡으면 그 사람은 존재하지 않는 규칙 체계를 배우고 그 오해는 조용히 오래 간다. 실측: 규칙표에 R14가 생겼는데 소개 문서는 R13까지였다 | "규칙을 늘렸는데 설명은 그대로인가 — 문서가 인용한 숫자는 아직 맞나?" | 소개 문서에 새 규칙을 설명하고 인용 수치를 갱신(해소는 문서 편집뿐 — 면제 경로를 두지 않는다) |
| **R16 순차 프로세스 SSOT** | 사슬 문서 변경·주기 | `check-process-ssot`(여러 스펙에 걸친 **순차 사슬**을 한 문서가 전 구간 소유하는가 — ①선언된 단계 전부가 SSOT 문서에 있는가 ②단계를 둘 이상 담은 다른 문서가 SSOT를 참조하는가 ③실행 사이의 비교·합의를 요구하는 단계가 기록이 만날 **저장소를 선언**하고 그 저장소가 **소유**되는가. `processes` 미선언이면 inert, `processSsotPolicy` off·advisory(기본)·hard, SPEC-047). **실측:** 8단계 close-out 사슬의 조각이 6개 문서에 흩어져 전 구간 문서가 없었고, 그 흩어짐이 코드의 무행동으로 나타났다 — 교차검증이 상대 기록 없으면 통과했고 두 기록이 만날 저장소가 아예 없어 **비교가 한 번도 수행된 적이 없다** | "이 사슬을 전부 담은 문서가 있나 / 비교 단계의 기록은 어디서 만나나?" | 전 구간을 한 문서에 모으고 조각 문서는 그것을 참조. 저장소를 선언하고 어느 스펙의 Files에 편입 |
| **R17 감시자 실재** | 채택·주기·CI | `check-watchdog`(**감시자 자신이 실재하는가** — ①**우회 불가한 채널**: CI 파일에 스윕 호출이 있는가(로컬 훅은 `--no-verify`로 우회되고 웹 UI 머지는 훅을 아예 타지 않으며 게이트 파일은 지워도 조용하다 — 커밋한 사람이 끌 수 없는 것은 서버측 CI뿐이다) ②**채택 영수증**: `sdd/adoption.json`(설치기가 남기고 **커밋한다** — 실행 원장과 달리 세션 상태가 아니라 채택 선언이다)이 실재하고 그것이 선언한 게이트가 지금도 있는가. `watchdogPolicy` off·advisory(기본)·hard, SPEC-048). **왜 필요한가:** 무시는 순환 때문에 안 잡힌다 — 무시하는 프로젝트는 게이트를 안 돌리고, 그러면 게이트가 무시를 고발할 기회가 없다. 실측: 제보 6건 중 5건이 **이미 고쳐진 결함**이었고 그 프로젝트는 낡은 킷으로 몇 달을 갔다 | "이 저장소에서 강제가 실제로 살아 있나 — 아니면 조용히 벗어났나?" | `sh scripts/sdd-init.sh`(CI 워크플로·영수증을 **필수 산출물로** 생성) → 영수증 커밋 → 낡았으면 `prompts/update.md` |
| **R18 배선 무결성** | 설치된 게이트 집합 | `check-import-wiring`(**게이트가 애초에 로드되는가** — 로컬 import의 ①대상 파일이 실재하는가 ②요구된 **export가 실재하는가**를 전이적으로 대조. 판정 시작점은 게이트 자신의 디렉터리에 있는 모듈 전부이고, 0건이면 INERT다(Python 런타임 전용 설치 — 0건은 '깨끗함'이 아니라 '볼 것이 없음'). 파서가 확정 못 하는 export 형태는 **확인 못 함**으로 자백하며 위반으로 세지 않는다. `importWiringPolicy` off·advisory(기본)·hard(킷), SPEC-050). **왜 필요한가:** 실측 제보 — `update.md` diff가 공유 lib 27개를 빠뜨려 게이트는 최신·lib은 구판인 **부분 동기화**가 됐고 소비처는 판정이 아니라 `SyntaxError: … does not provide an export named …`를 받았다. 파일이 없는 것도 아니어서 배포 폐포 계약(SPEC-004)으로도 안 잡힌다 — 그건 *파일 실재*, 이건 *export 실재*다 | "내가 깐 게이트들이 서로 아는 사이인가 — 아니면 절반만 새것인가?" | 정본에서 그 모듈 재복사(면제 경로 없음) |
| **R19 에이전트 배선** | 에이전트 설정 | `check-agent-wiring`(**감시자가 에이전트를 보는가** — 선언(`agent-hooks.list`, 설치기와 게이트의 단일 선언)의 각 훅이 에이전트 설정에 ①배선됐는가 ②매처가 좁아 특정 도구에서 빠지지 않았는가(**부분집합** 판정 — 넓히는 것은 정상) ③지목된 스크립트가 실재·읽기 가능한가. 설정 파일 부재·깨진 JSON은 각각 다른 사실로 낸다. 선언 부재·0건은 INERT. `agentWiringPolicy` off·advisory(기본)·hard(킷), SPEC-051). **왜 필요한가:** R17은 **커밋 이후** 채널(CI)을 본다 — 에이전트가 스펙 없이 코드를 쓰고 있는 그 순간을 보는 층이 아니다. 그 순간을 보는 것은 에이전트측 훅뿐인데 그 배선이 **아무 축의 대상도 아니었다.** 실측: 킷 자신에 `.claude/`가 통째로 없었고 이 층만 도그푸딩 0이었는데 R17은 초록이었다 | "지금 이 에이전트를 누가 보고 있나 — 아니면 혼자 날뛰는 중인가?" | `sh scripts/sdd-init.sh`(선언 기반 배선·기존 훅 보존·배선 직후 자기검증) |
| **R20 명세 모순 감사** | 명세 코퍼스 전체 | `check-spec-conflict`(**코퍼스가 스스로와 정합한가** — 같은 대상에 한 스펙은 `SHALL`, 다른 스펙은 `SHALL NOT`을 걸었는가. 판정: 상반 극성 + 술어 내용 토큰의 **포함 관계** + 공유 토큰에 **말뭉치 희귀 토큰** 존재(흔한 술어의 주어-무관 겹침을 어휘 목록 없이 통계로 배제). 한 스펙 내 모순과 교차 스펙 모순을 **따로** 센다(해소 방법이 다르다). SHALL 지시 0건·스펙 0건이면 INERT. `specConflictPolicy` off·advisory(기본)·hard(킷), SPEC-052). **왜 필요한가:** 앞의 규칙들은 "에이전트가 명세대로 하는가"를 본다 — 이 축만 **명세 자체**를 본다. 실측: 명세 안에 반대 방향 지시가 공존한 탓에 소유자가 여러 세션에 걸쳐 금지한 경로가 재발했고, 두 지시 다 다른 모든 게이트를 통과하고 있었다 | "급할 때 에이전트가 어느 쪽을 따를지 명세가 정해주는가 — 아니면 먼저 본 쪽인가?" | 어느 지시가 정본인지 결정해 한쪽을 고친다(게이트는 정하지 않는다·면제 없음) |
| **R21 진단 가드** | 진단 명령·선언 | `check-diagnosis-guard`(**조사 전에 명세를 보는가** — 훅 모드: 선언된 명령 패턴에 `surface`는 관련 명세와 **절 위치**를 띄우고 `deny`는 exit 2로 막는다. 스윕 모드: **선언 자체를 판정한다**(깨진 정규식·부재 스펙·사유 없음·대안 없는 금지 — 잘못된 선언은 아무것도 막지 않고 아무것도 알리지 않는다). **명세를 읽는 명령은 모든 규칙을 이긴다.** `diagnosisGuardPolicy` off·advisory(기본)·hard(킷), SPEC-053). **왜 필요한가:** "조사 전에 명세를 읽었는가"는 정적으로 판정되지 않고 **조회는 커밋도 파일 변경도 남기지 않는다** — 커밋 게이트로는 원리상 볼 수 없다. 실측: 에이전트가 명세에 답이 있는데 읽지 않고 실측으로 다시 찾아 **결론까지 틀렸고**, 오너가 여러 세션에 걸쳐 금지한 경로가 재발했다 | "이 조회의 답이 이미 명세에 있지 않나?" | 그 명세의 Edge Cases·Change Log를 읽는다 · 금지를 걷어내려면 명세를 고친다(우회 아님) |
| **R22 완료 판정 신호** | 완료를 주장하는 검사 선언 | `check-completion-signal`(**완료 판정이 대상 상태를 봤는가** — 신호 3종 `target-state`·`derived`·`self-report`, 순서가 곧 등급이다. 완료의 하한은 `target-state`이고 **파생·자기신고만으로 완료를 주장하면 위반**이다. 미선언은 추정하지 않는다(기본값을 두면 그 기본값이 조용히 정답이 된다). 완료 주장 0건이면 INERT. `completionSignalPolicy` off·advisory(기본)·hard(킷), SPEC-055). **왜 필요한가:** 실측 제보 — 파이프라인 로그에 성공 줄이 있고 CI가 초록이어서 **배포 완료로 보고했는데 migrate Job 실패로 배포 스테이지가 스킵된 상태**였다. 파생 신호는 대상이 실패했을 때도 정상일 수 있고, 스킵되면 실패 줄조차 남지 않는다. SPEC-031(증거 자산 등급)과 **직교**한다 — 브라우저 등급 자산의 로그를 읽어 완료를 말한 경우가 그쪽에서는 표현되지 않는다 | "이 완료 판정은 대상을 봤나, 대상에 대한 이야기를 봤나?" | 대상을 조회하는 검사를 하나 더한다(선언만 고치면 그 선언이 거짓이 된다) |
| **R23 FR 배치** | spec 커밋(commit-msg, spec-sync 뒤) | `check-fr-placement`(**FR 정의가 `## Functional Requirements` 섹션 안에 있는가** — 줄 시작 `- **FR-NNN**`·`**FR-NNN**`만 정의로 본다(Change Log 표 행·산문 참조는 이력·인용이라 제외). FR 섹션이 아예 없는 스펙은 exempt. 위반 시 FR id·있던 섹션명·줄 번호를 낸다. **훅은 자동 교정하지 않는다** — `--fix`는 사람이 명시적으로 실행하며, 이동 시 흡수 범위는 FR 줄에 **바로 이어지는** `>` 인용 줄까지만이다(다른 문단의 인용을 쓸어가지 않는다). 차단 시 실패 클래스 `fr-outside-section`을 선언(R24 소비). `frPlacementPolicy` off·advisory(기본)·hard, SPEC-056). **왜 필요한가:** 실측 제보 — 에이전트가 하루에 같은 실수를 세 번 했다(FR을 Dedup-Review·Ownership 섹션에 썼다). 파일은 멀쩡히 파싱돼 사람이 그 자리에서 알아차릴 수 없었고, 어긋남은 **다른 게이트가 커버리지를 셀 때** dangling `@covers`로 뒤늦게 드러났다 | "이 FR 정의, 진짜 FR 섹션 안에 있나?" | `check-fr-placement.mjs --fix` 실행 후 diff 확인·커밋(자동 적용 아님) |
| **R24 게이트 실패 에스컬레이션** | commit-msg(R23 뒤) | `check-gate-escalation`(**반복이 기억되는가** — 임의 판정 게이트가 차단할 때마다 `armVerdict` 공통 종료 훅이 원장(`.sdd/gate-failures.jsonl`)에 한 줄을 자동 append한다(게이트별 손배선 0건 — 새 게이트도 자동 참여). 클래스는 게이트의 **명시 선언**(`verdict(kind, detail, {class, target})`)일 때만 집계되고, 텍스트 추론은 하지 않는다(추측 금지). 같은 (게이트,클래스)가 임계치(기본 3) 이상인데 전용 가드가 없으면 "가드를 만들어라"를 명시한다. 가드 선언 자체의 결함(4필드 누락·사유 없음·가드 파일 부재)은 정책 강도와 무관하게 항상 차단한다(조용한 무발화 금지). `gateFailureEscalationPolicy` off·advisory(기본)·hard, SPEC-057). **왜 필요한가:** 실측 제보 — 게이트는 같은 실수를 세 번 다 정확히 잡았지만 "이게 오늘 세 번째"라는 정보가 어디에도 없었다. 목적은 벌이 아니라 가시성이다: 세 번째에 이 게이트가 뜨면 그날의 반복은 애초에 없었다 | "이 실패 클래스, 오늘 몇 번째인가 — 전용 가드가 있나?" | `gateFailureGuards`에 새 가드를 등록하거나(그 전에 실제로 만든다) 전용 게이트를 설계한다(자동 생성 아님 — 판단은 사람 몫) |
| **R25 위험 행동 승인** | 도구 호출 직전(PreToolUse Bash) | `check-risky-action`(**되돌리기 어려운 행동이 독립 검증 없이 지나가는가** — 프로젝트가 선언한 `riskyActionPatterns`(`{match, class, verifyAgainst, why}`)에 매칭되는 명령(트래커 상태 전이·배포·파괴적 DB 조작 등)을 만나면, 행동 문자열의 sha256 해시에 결속된 **승인 마커**(`.sdd/action-approvals.jsonl`)가 있고 신선(`riskyActionApprovalTtlSeconds`, 기본 900초)한지만 결정론적으로 본다. **게이트는 서브에이전트를 스스로 부르지 않는다** — 차단 메시지가 "별도 컨텍스트의 서브에이전트로 확인 후 `--record`로 마커를 남겨라"를 지시할 뿐이고, 실제 호출은 차단당한 실행기가 자기 도구로 한다. 마커가 세션·패턴 단위가 아니라 **정확한 행동 해시**에 결속되는 이유: 한 번의 승인이 다른 대상의 같은 종류 행동까지 통과시키는 것을 막는다. 차단 시 실패 클래스를 선언(R24 소비). `riskyActionPolicy` off·advisory(기본)·hard, SPEC-058). **왜 필요한가:** 실측 제보 — QA 실측·트래커 댓글까지 마친 티켓을 아직 배포도 안 된 채 종결 상태로 넘기려 했다. 커밋 이전, 대화 안에서 끝나 기존 커밋 게이트(R1~R24)가 원리상 관여할 지점이 없었다 | "이 행동, 독립 검증 마커가 있고 신선한가?" | 별도 컨텍스트 서브에이전트로 확인 후 `check-risky-action.mjs --record`로 마커를 남기고 재시도(자동 승인 아님) |

> **표를 읽는 법 — 게이트가 초록이라고 다 본 것이 아니다.** 스윕은 규칙마다 세 상태를 낸다: `✓ 판정함`(봤고 위반 0) · `· 안 봄(판정 안 함 — 통과 아님)`(정책 off이거나 판정 입력이 없다) · `⚠ 확인 필요`(위반이거나 게이트가 판정 종류를 선언하지 않았다). **초록의 자격은 "판정함"에만 있다.** 요약 줄이 언제나 분모를 밝힌다 — `게이트 N종 = 판정 M · 안 봄 K(off·inert·생략) · 미판정 J`. 이 계약은 SPEC-040 소유이며, 각 게이트가 자기 종류를 선언하지 않으면 스윕이 `미판정`으로 세어 초록이 되지 않는다.

> **범위 밖(의도적):** `check-derivation`(재도출 소스 회계)·`sdd-smoke-scan`(증거 드리프트)은 이 하네스의 규칙표에 넣지 않는다 — 트리거가 "spec/코드 변경"이 아니라 **재채택(readopt)·증거 갱신** 이벤트라서다. 실행 지점은 readopt 절차(`prompts/readopt.md` 6~7단계)와 CI 스텝(`ci-examples.md`·`sdd-gates.yml` 주석)이 담당한다. `retag`는 게이트가 아니라 마이그레이션 도구(dry-run 기본)라 detect 대상이 아니다.

## 실행기 (Claude Code 1차 — 다른 에이전트는 같은 표로 자체 구현)
- **detect 집계:** `node scripts/sdd-sync.mjs [--strict]` → 규칙별 sync 리포트.
- **인터랙티브:** 스킬 `/sdd-sync` — 리포트 → 규칙별 사람 의사 확인 → act.
- **상시(R4·R5):** git pre-push 훅이 `sdd-sync.mjs`를 advisory 실행 → drift면 `/sdd-sync` 안내(기본 비차단, `SDD_SYNC_BLOCK=1`로 차단). R5(test 실행)는 같은 집계에 포함되고, 서버측 백스톱은 CI(킷 자신은 `.github/workflows/sdd-gates.yml`, CICD-001)가 담당.
- **병합 시점:** `pre-merge-commit` 훅이 pre-commit과 같은 품질 게이트를 무충돌 병합에도 실행 — 두 브랜치가 같은 스펙 번호·같은 ownership 키를 각자 들고 깨끗이 병합돼 main이 사후 red가 되는 경쟁 차단(감사 M5, `sdd-init`·self-hooks 배선).
- **비-Claude 에이전트(Kiro·Codex 등):** 슬래시 명령·SessionStart 주입은 Claude Code 편의 계층이다 — 강제(게이트+git 훅)와 절차(`prompts/`)는 실행기 무관이므로 슬래시를 못 써도 **같은 절차를 수동으로** 밟고 되묻지 않는다. 방법론 상시 주입은 그 에이전트의 상시-로드 문서(Kiro `.kiro/steering/sdd.md`·기타 `AGENTS.md`)에 이 규칙표/`sdd-session-context.sh` 출력을 옮겨 대체한다. 수동 첫-스펙 절차는 `prompts/adopt.md` §"에이전트 무관 실행".

## 불변
- 어느 방향도 **자동 덮어쓰기 금지** — 사람 의사 확인 게이트 필수.
- 탐지는 advisory 1차; `--strict` 승격은 팀 선택.
- **게이트 stdout이 판정의 정본** — 집계기는 게이트 stdout을 `⚠`/`✗`로 스캔한다. 그래서 게이트는 자기 판정 줄만 stdout에 쓰고 하위 프로세스(테스트 러너 등) 출력은 stderr로 보낸다. 어기면 green이 ⚠로 읽힌다(감사 M-8 실측: 킷 테스트 *이름*의 ⚠·✗ 31줄에 R5가 걸려 항상 "확인 필요" — 초록이 경고로 읽히면 사람이 ⚠를 무시하는 습관을 들여 진짜 경고를 놓친다).
- **라이브에 반영된 것은 커밋 전이라도 spec Change Log에 먼저 착지해야 한다** — 강제 지점은 커밋만이 아니다. `kubectl apply`·`helm upgrade`처럼 **배포가 커밋보다 먼저**인 궤도에서는 commit-msg 훅까지 아무 신호가 없어 그 사이 spec↔live 드리프트가 누적된다(실측: INFRA-005 역방향 흡수). PostToolUse 배포 가드(SPEC-035)가 배포 직후 상기시키되 **차단하지 않는다** — 이미 실행된 것을 막는 척하는 게이트는 신뢰를 잃는다. 차단은 커밋·CI가 계속 담당한다. **막을 수 있는 것은 사전에 막는다 — 사후 상기가 원칙인 것과 늦는 것은 다르다.** 스펙 드리프트는 배포 후에야 알 수 있지만, **"이 배포가 재현 가능한 리비전에서 나오는가"**(미커밋 트리·upstream 뒤처짐)는 순수 git 조회라 배포 **전에** 판정된다 — 그래서 그 축만 PreToolUse로 앞당겨 실제로 차단한다(`deployPreconditionPolicy`, SPEC-035 FR-006). 실측: 가드가 `terraform apply`를 정확히 감지하고도 막지 못했고, 사후 상기는 같은 세션의 **두 번째 apply**도 막지 못했다. 단 **모르는 것은 hard에서도 차단하지 않는다**(upstream 없음=미판정) — 오탐이 잦은 사전 차단은 사람이 훅을 꺼버린다. **승인한 것과 적용되는 것이 같아야 한다** — 실측 사고에서 프로덕션을 두 번 죽인 삭제는 **plan에 있었다**(아무도 보지 않았을 뿐이다). 대화형 승인이 diff를 보는 유일한 지점이라, `저장된 plan 없는 -auto-approve`는 "승인"이라는 개념 자체를 없앤다(저장된 plan을 적용하는 auto-approve는 정당하다 — CI가 그 형태다). **삭제는 갱신과 같은 강도로 다뤄지면 안 된다** — `terraform destroy`는 감지 목록에 아예 없었다. 파괴적 명령은 `SDD_DESTROY_OK=1`로 **매 실행 동의**를 요구한다(standing policy가 아니라 per-invocation 선언이라야 흔적이 남고 습관이 되지 않는다). **그리고 배포 명령의 성공은 서비스의 생존이 아니다** — `exit 0 ≠ 판정했음`의 배포판 사촌이다(실측: apply 성공·CI 초록·전 요청 403). `deploySmokeCommand` **미선언 자체를 부채로 계상**한다: 아무도 확인하지 않은 침묵과 확인해서 살아 있는 침묵이 같아 보이면 안 된다. **`advisory`와 `hard`의 차이는 부채 적재다** — 배포 시점의 비차단은 두 강도가 같지만(막을 대상이 없다), `hard`는 미기록 배포를 부채 파일에 적재하고 pre-commit이 그 잔여로 커밋을 막는다. 터미널 경고는 스크롤과 함께 죽고 파일은 남는다 — 승격해도 아무것도 달라지지 않는 정책은 승격 대상이 아니라 장식이다(실측 제보: 둘이 구분 불가능했다). 부채는 **소유 스펙 Change Log 행이 스테이징되는 순간에만** 자동 해소된다(파일을 지우는 것은 갚는 것이 아니다).
- **우회를 유발하는 강제는 강제가 아니다** — 훅이 수십 초 걸리면 사람은 `--no-verify`로 우회하고 그 순간 훅 전체가 무의미해진다(실측: 스윕 30.3초 중 R5 스위트 실행이 29.8초, 나머지 10규칙 0.5초 → 매 push가 멈춰 우회가 습관이 됐다). 훅 경로는 **몇 초 안에** 끝나야 한다. 다만 빼는 것은 **선언적 위임**이어야 한다 — `syncHookRules`로 실행 규칙을 적고 `syncHookDelegatedTo`로 담당자를 밝히면(없으면 에러) 매 실행 "위임 — 담당: …"이 출력되므로 사유 있는 skipped와 같다. 시간 예산 초과분은 위임이 아니라 **미판정**으로 flagged된다(조용한 통과 금지).
- **완화를 선택지로 내밀지 않는다** — 위반 해소안을 제시할 때 강도 하향·수치 임계 상향·면제 추가를 **권장안으로 올리지 않는다**(실측: 캡 10을 FR 12개가 넘기자 "캡을 12로 상향"이 권장안으로 제시됐다). 자를 늘려 재는 것은 해소가 아니라 회피다. 정당한 해소는 분할·병합·스펙 편집이고, 진짜 재조정은 `policyRatchetExceptions`로 부채를 표면화한다 — 래칫이 강도·임계 양쪽을 감시하므로 조용한 완화는 어차피 exit 1이다.
- **범위 축소·이월도 권장으로 내밀지 않는다 — 배치(순서)와 범위(무엇을)를 섞지 마라.** 확인된 결함의 해소안을 낼 때 "일부만 지금 고치기"를 **권장**으로 올리면, 나머지 확인된 결함을 **안 고치는 것이 기본값**이 된다(실측: 데이터로 확인된 CRITICAL/HIGH 취약점 앞에서 "read측 치명상 4건만 (권장) / 전부 / 스펙에만 기록"이 제시됐다 — 권장이 부분 수정이었다). "한 라운드에 검증 가능한 크기"는 옳은 공학이지만 그건 **어떻게 나눠 착지시킬지(순서)**이지 **무엇을 고칠지(범위)**가 아니다. 기본 권장은 **확인된 것 전부**이고, 크기는 배치로 쪼갠다(한 스펙 = 한 커밋, 빅뱅 금지). 진짜로 이월한다면 회피가 아니라 **결정**이어야 한다 — 무엇을·왜·언제까지·누가 위험을 수용했는지를 함께 적는다(사유 없는 이월은 미수정을 문서 형태로 세탁하는 것이다). 보안 결함은 특히: "스펙에만 기록"은 취약점을 라이브에 남긴 채 기록만 남기는 것이라, 수용자와 기한 없이는 선택지가 아니다.
- **교착의 해소는 캡을 푸는 것이 아니라 출구를 만드는 것이다** — 규범 A가 규범 B의 유일한 해소 경로를 막으면, 남은 선택지는 완화뿐이 되고 완화는 위 불변이 금지한다. 그 상태는 사람의 잘못이 아니라 **규칙 집합의 결함**이다(실측 제보: aggregate를 가질 수 없는 계층이 FR 캡을 넘겼을 때 `entity(min)`이 분할을 막아, 남은 출구가 캡 상향뿐이었다). 고치는 방향은 **막힌 출구를 여는 것**이지 캡을 푸는 것이 아니다 — `supportLayerSpecs`는 `entity(min)` 하나만 면제하고 모든 캡을 그대로 둔다. 그리고 새로 연 출구는 **사유 필수 + 무결성 검사 + 상시 표면화**를 달고 나온다(면제는 조용히 '완료'가 되지 않는다).
- **같은 주장에 대한 두 개의 선언은 반드시 대조된다 — 그리고 등급은 라벨이 아니라 경로로 판정한다.** 본문의 `[미확인]`과 매니페스트의 실측 증거가 동시에 참인 채로 통과하면, 두 기록 모두 신뢰를 잃는다(실측 제보: `[미확인]` FR이 smokeManifest에 증거를 갖고 있었고 아무도 대조하지 않았다). 게이트는 어느 쪽이 맞는지 모른다 — **모순을 지목하고 하나를 고치게 한다**(R8, SPEC-031 FR-007). 관련해서 검증 방법을 주장별 라벨(`@verifies method=browser` 등)로 선언하는 설계는 **기각한다**: 파일 위치는 게이트가 검증할 수 있고 라벨은 자기신고다. 라벨을 도입하면 이 방법론이 존재하는 이유(산문 자기신고로 소비된 `[검증]`)를 한 층 위에 재생산한다. 관례 밖 경로는 config(`browserEvidencePatterns`·`verificationKinds`)로 넓힌다 — config는 한 번 선언하고 리뷰에 걸리지만, 라벨은 매 주장마다 반복되는 자기신고다.
- **실재는 동일성이 아니다 — 그리고 판정 범위를 좁히는 것은 귀속을 고치는 것이 아니다.** 태그가 가리키는 FR이 *실재*하는 것과 그 FR이 *이 테스트를 인정*하는 것은 다른 사실이다: 번호가 겹치기만 하면 단방향 검사는 통과한다(실측: 다른 세션이 무관한 기능을 같은 번호로 착지시킨 순간 위반이 사라지고 회계가 "커버됨"을 보고했다 — 두 세션에 걸친 diff라 사람 눈에도 안 띈다). 그래서 FR 쪽 `[검증]` 목록을 대조 축으로 양방향을 닫는다(R1b, SPEC-039). 표기가 없는 FR은 **위반이 아니라 부채**로 따로 센다 — 도입 소음이 본 신호를 덮으면 사람이 정책을 끈다. 그리고 게이트가 워킹트리를 보고 commit-msg가 staged를 보는 어긋남은 **범위 knob으로 풀지 않는다**: `staged`로 좁히면 커밋 밖 dangling이 영구히 안 보이고 그 손실은 완화다. 판정 집합은 전역으로 두고 **강도를 귀속으로 가른다** — 커밋 밖 위반은 advisory로 낮춰 오귀속 차단(과 그것이 유발하는 "파일을 옮겨 커밋" 우회)을 없애되, staged를 알 수 없는 CI에서는 전부 막는다.
- **병렬 격리는 편집 금지이지 읽기 금지가 아니다 — 그리고 중복은 선언에만 있는 것이 아니다.** dedup이 촘촘한 것은 *스펙 중복*이고, *구현 중복*은 오래 사각이었다(실측: 병렬 서브에이전트가 같은 규칙을 세 갈래로 만들었고 **같은 파일에 이름만 다른 export 두 개**가 공존했는데 게이트 4종 전부 green). 유발 조건(격리 지시 + 동시 upstream + 각자 성실한 헬퍼 생성)은 병렬 실행을 권장하는 방법론에서 **예외가 아니라 정상 경로**다 — 사람의 주의로 막을 수 없다. 규범은 두 줄이다(principles §5b): 새 헬퍼 전에 기존 export를 grep한다(읽기는 금지되지 않았다), 통합자는 머지 후 중복 점검을 체크리스트로 한다. 기계 백스톱은 R13이고, **결정적 층만 차단한다** — 구조 중복 판정에 파서가 필요한데 킷이 그것을 번들하면 의존성 0과 언어 무관을 동시에 잃으므로 어댑터로 위임하고 비차단으로 둔다.
- **확률적 판정에는 차단력을 주지 않는다** — LLM·임베딩이 낸 후보로 빌드를 깨면 그 오탐이 곧 방법론의 오류가 된다. 확률적 층은 "무엇을 볼지"만 정하고 "무엇이 참인지"는 사람이 결정적 층(정본 선언·기각 원장)으로 착지시킨다. 이건 산문 규범이 아니라 **코드 분기**이며 회귀 테스트로 고정된다(R10 ③, SPEC-033 FR-006). 같은 이유로 후보 목록의 낡음도 차단하지 않는다 — 커밋마다 재생성을 요구하면 사람이 그 층을 통째로 떼어낸다(회피를 유발하는 강제는 강제가 아니다).
- **`exit 0` ≠ "판정했음" — 그리고 그 사실은 산문이 아니라 타입으로 말한다.** 게이트가 한 줄도 출력하지 않으면 clean이 아니라 **미판정**(flagged)이고, 규칙표가 선언한 게이트가 설치돼 있지 않은 것도 미판정이다(실측: 비-ASCII 경로에서 `check-test-run`이 무음 exit 0이라 `runTestsPolicy: hard`가 여러 라운드 거짓 green이었고 집계기는 clean으로 읽었다). 이 규범은 오래 **산문으로만** 존재했고 그래서 하네스 자신이 어겼다 — 게이트가 "off (판정 안 함)"이라 적은 줄을 집계기가 `/[⚠✗]/` 스캔으로 훑고 `✓ clean`으로 분류했다(실측: R7 Engines·Events, R9 라이브 대조). **고칠 것은 어휘가 아니라 반환 타입이다.** 게이트는 자기 판정의 **종류**를 선언한다 — `JUDGED`(봤고 결과를 안다) · `OFF`(사람이 껐다) · `INERT`(켰는데 볼 것이 없다) · `SKIPPED`(사유 있는 생략) · `UNTYPED`(선언 안 함 = 배선 누락). **초록의 자격은 JUDGED에만 있다.** 종류는 다섯 개뿐이고 늘리지 않는다 — 늘리면 "이건 어디에 넣지"가 생기고 그 자리가 곧 예외다. `OFF`와 `INERT`를 가르는 이유는 해소가 다르기 때문이다(켜면 됨 vs 입력을 만들어야 함); 한 상태로 묶으면 "켰으니 됐다"가 성립해 *hard 선언 + 무판정 = 거짓 안전*이 되살아난다. 스윕은 규칙을 **세 상태**(판정함 · 안 봄 · 위반)로 렌더하고, 요약은 **언제나** 집계를 낸다 — `게이트 N종 = 판정 M · 안 봄 K(off·inert·생략) · 미판정 J`. 초록의 분모를 같은 줄에서 밝히지 않으면 "전부 sync ✓"를 읽은 사람은 안 본 것까지 검증됐다고 읽는다(SPEC-040).
- **틀이 있는 것과 그 틀이 이 산출물을 본다는 것은 다른 사실이다 — 그리고 등록은 실행과 함께 꺼지면 안 된다.** R9(라이브 대조)라는 틀이 있어도 **새 배포 산출물을 선언할 때 대응 검사 등록을 요구하지 않으면** 그 산출물은 아무도 안 본다(실측 제보: qa에이전트 도입에서 배포 산출물 결함 8건을 **배포로 하나씩** 발견했고 — ECR 리포 없음·크로스계정 ECR API·**아치 불일치(arm64 노드에 amd64 이미지)**·**이미지에 의존 모듈 누락** 등 — 전부 저장소 밖 사실이며 전부 로컬 게이트를 green으로 통과했다. 검사 항목 6건에 그 중 하나도 없었다). 그래서 R9는 **두 축**이다: **등록**(선언된 배포 산출물에 담당 검사가 있는가 — 순수 선언 대조라 오프라인에서 판정된다)과 **실행**(그 검사가 실제로 라이브를 봤는가 — 자격증명 필요). ⚠ **정책도 갈라야 한다**: 실행은 자격증명이 없어 흔히 off·skipped이고 훅에서 위임되는데, 한 정책에 묶여 있으면 실행을 끄는 순간 **등록 누락까지 함께 안 보인다**. 담당 선언(`covers`) 없는 검사는 아무것도 커버하지 않는다 — 그걸 커버로 세면 "검사가 하나라도 있으면 통과"가 되고 그게 바로 제보의 상태다. `deployArtifactMarkers`는 조용한 기본값을 두지 않는다(무엇이 배포 산출물인지는 프로젝트 어휘다 — 미선언은 inert). 표준 검사 4종(아치 일치·레지스트리 계정과 리포 정책·엔트리포인트 dry-run·스테이지 전제 자원)은 `sdd.config.presets.md`에 붙여넣을 형태로 있다 — 프로젝트마다 재발명하지 않는다.
- **"단위테스트 통과"와 "배포본에서 실제 실행됨"은 같은 증거가 아니다.** 아치 불일치·이미지 안 모듈 누락은 저장소 안 테스트가 100% green이어도 남는다 — 깨지는 곳이 배포된 이미지의 런타임이고 테스트는 거기 닿지 않기 때문이다. 그래서 **배포 산출물을 소유한 스펙**의 배포 대상 주장은 배포 등급 증거(smoke·e2e·live·runbook 또는 실행 원장 기록)를 요구한다(R8, SPEC-031). ⚠ 트리거는 **소유와 주장 둘 다**다 — 마커만으로 걸면 배포를 *다루기만 하는* 스펙(가드의 판정 로직 등)까지 잡혀 킷 시운전에서 31건이 쏟아졌고, 그런 스펙의 정답 증거는 단위테스트가 맞다. **소유 없는 마커는 화제이지 대상이 아니다.**
- **존재는 실행이 아니다 — 그리고 포기는 허용하되 침묵은 금지한다.** `[검증: <경로>]`가 실재하는 자산을 지목하는 것(SPEC-031)과 그 자산이 **돌았다는 것**은 다른 사실이다. 실측 제보: ①에이전트가 "재현 데이터가 없다"며 실측을 중도 포기했는데 아무 게이트도 걸리지 않았고, ②검증 러너가 대상 0건으로 `exit 0` 종료해 성공과 무행동이 동형이 됐으며, ③배포 훅 Job이 참조 이미지 부재로 **한 번도 뜨지 않았는데** 비차단이라 파이프라인은 SUCCESS였다. spec↔code 정합만 보는 층에서는 이 셋이 전부 초록이다. 그래서 **실행 원장**(`verificationRunLedger`, JSONL)을 판정 입력으로 두고 선언된 증거와 대조한다(R14, SPEC-041) — 러너·CI 스테이지·에이전트가 `--record <asset> <outcome> [사유]`로 자기 결과를 남기고, `outcome`은 SPEC-040의 다섯 종류를 그대로 쓴다(어휘가 갈라지면 "안 봤다"가 두 뜻을 갖는다). **차단하는 것은 침묵(기록 없음)과 깨진 기록(사유 없는 포기)뿐이다** — 사유가 붙은 미실행은 어떤 강도에서도 막지 않고 매 실행 부채로 표면화한다. 막으면 사람이 사유를 지어내고, 그 순간 원장이 거짓말을 담기 시작한다. 비차단 스테이지를 두는 것 자체는 정당하지만 **그 스테이지가 안 떴다는 사실이 결과에 드러나야** 하고, 전제 자원(이미지·시크릿·리포지토리)의 존재는 스테이지 자신이 검사해 없으면 사유와 함께 남긴다.
- **"0건"은 무엇의 0건인지 밝혀야 한다 — 킷 기본값은 킷의 언어다.** 러너·게이트가 처리 대상 0건으로 끝나면서 성공과 같은 신호를 내면 "무행동"이 "성공"과 동형이 된다(실측 제보: 검증 러너가 대상 0건으로 exit 0, 배포 훅 Job이 전제 자원 부재로 한 번도 안 떴는데 파이프라인은 초록). 킷 안에서 이것이 나타나는 가장 흔한 얼굴은 **언어 편향**이다 — `duplicateLiteralFileRegex` 기본값이 JS/TS라서 Python·Terraform 저장소에서 아무 파일도 안 보고 "중복 0건"을 보고했고, 그 0은 진짜 0과 구분되지 않았다. 그래서 판정 입력이 없거나(소스·매니페스트·명령·역할 미선언) 언어 미선언으로 **저장소 일부만 본 상태**는 `INERT`이고, 게이트는 무엇을 안 봤는지(확장자·건수) 지목한다. 기본값을 없애지는 않는다 — 그러면 기존 채택 프로젝트가 통째로 inert가 되어 채택이 후퇴한다. 대신 기본값에 기대는 동안 **매 실행 무엇을 안 봤는지 말한다**.
- **연결의 존재는 연결의 진위가 아니다 — 태그가 있다는 것과 그것이 시험한다는 것은 다른 사실이다.** 실측 제보에서 스펙과 코드가 어긋난 채 모든 게이트가 초록을 유지한 사례 세 건이 왔고, 공통 원인은 하나였다: 게이트는 **연결의 존재**만 보고 **진위**는 보지 않는다. 요구가 "Claude in Chrome(MCP)로 측정한다"고 주장하는데 커버 테스트는 선택자가 문자열 `chrome`을 돌려주는지만 확인했고(동어반복), 리눅스 1대 관측이 보편 규칙으로 승격됐는데 근거 칸이 비지 않아 통과했고, `process.env.BASE_URL || "https://…"` 한 줄이 배포 대상을 정하는데 소유는 파일 단위라 아무도 파일 **안**을 안 봤다. 의미 판정은 여전히 리뷰의 몫이므로(존재는 기계, 질은 리뷰) 그 경계 안에서 **값싸고 결정적인 조각 하나씩**만 가져간다 — 요구가 이름 댄 등록 용어가 커버 파일에 없다(R1c) · 환경을 지목한 관측에 범위 표기가 없다(R3) · env 폴백 기본값이 외부 대상인데 소유 스펙이 그것을 모른다(R1d). **좁히는 것은 소유의 입도가 아니라 결정의 종류다** — 모든 상수에 요구를 붙이면 스펙이 코드의 사본이 되고 그 순간 아무도 스펙을 읽지 않는다. 같은 이유로 EARS 문법에 범위 한정자를 강제하지 않는다: 이미 쓰인 수백 개 요구가 한꺼번에 위반이 되고 그 규모의 강제는 우회를 낳는다. 방아쇠 폭은 **자기적용 실측으로** 고른다 — 근거 범위를 "관측 주장"만으로 걸었더니 킷에서 77건/26스펙이 쏟아져 신호가 묻혔고, *관측 주장 × 환경 지목*의 곱으로는 2건이며 그 2건이 정확히 위험한 모양이었다(SPEC-042·043·044).
- **고발 장치가 고발 대상의 협조를 요구하면 그것은 강제가 아니다 — 우회 불가한 채널은 서버측 CI뿐이다.** 오너 실측: **각 프로젝트가 방법론을 무시한다.** 그 무시는 순환 때문에 잡히지 않는다 — 무시하는 프로젝트는 게이트를 돌리지 않고, 그러면 게이트가 무시를 고발할 기회 자체가 없다. 채널의 성질을 실측으로 정리하면: 로컬 훅은 `--no-verify`로 우회되고 웹 UI 머지는 훅을 아예 타지 않는다(SPEC-008), 게이트 파일은 지워도 아무 일도 일어나지 않는다(지운 사실을 지운 쪽이 알린 적 없다), **서버측 CI는 커밋한 사람이 끌 수 없다.** 그래서 "감시자가 반드시 생성된다"의 실체는 CI 배선이고, 설치기는 그것과 **채택 영수증**을 선택 단계가 아니라 **필수 산출물**로 남긴다. 영수증은 **커밋한다** — 실행 원장(SPEC-041)은 세션 상태라 커밋하지 않지만 영수증은 "이 저장소가 이 방법론을 채택했다"는 선언이고, 체크아웃마다 사라지면 선언이 아니다. 그리고 상류 대비 낡음은 **판정하지 않는다**: 네트워크 없이 알 수 없고 모르는 것을 위반으로 말하지 않는다 — 대신 채택 시점·킷 커밋을 매 실행 보여줘 사람이 판단하게 한다. 이 라운드가 그 무시의 대가를 증명했다: 소비 프로젝트 제보 6건 중 **5건이 이미 고쳐진 결함**이었고, 그 프로젝트는 낡은 킷으로 몇 달을 갔으며 낡았다는 사실을 아무도 알려주지 않았다(SPEC-048).
- **차단 분기가 필드에서 발화한 적이 없으면 그 규칙은 검증되지 않았다 — 정적 검사로는 원리상 잡히지 않는다.** 실측 제보(사례 6): 어떤 검증 도구에 "로컬·개발 판정이 일치할 때만 전이한다"는 규칙이 있었고 **명세·구현·단위테스트가 모두 정상**이었다. 그런데 두 기록이 만날 저장소가 아예 없었으므로(로컬은 작업 디렉터리, 클러스터 Job은 볼륨 없는 파드의 `/tmp`) **그 교차검증은 단 한 번도 비교를 수행한 적이 없다** — 소유자 결정으로 도입된 규칙이 몇 달간 명세에만 존재했다. 결함은 **배선**에 있었고 코드를 읽는 어떤 검증기도 "정상"이라 답했을 것이다. 그런데 증거는 이미 매 실행 로그에 있었다: `교차검증: 상대 환경 판정 기록 없음 — 대조 생략`. "생략"이 통과처럼 읽혔고, **그 값이 몇 달간 한 번도 달라지지 않았다는 사실**을 신호로 읽는 장치만 없었다. 그래서 차단 분기는 자기 발화를 원장에 남기고, 세 사실을 **따로** 회계한다 — **미관측**(배선 없음) · **발화 0회**(경로가 안 돔) · **단조**(사유가 한 종류뿐, 값이 한 번도 달라지지 않음). 셋을 묶으면 사람이 무엇을 고쳐야 할지 모른다. 이 축은 **어떤 강도에서도 차단하지 않는다**(원장은 세션 상태라 신선한 체크아웃에서 항상 0건이고, 그걸 막으면 정책이 꺼진다) — 대신 매 실행 부채로 표면화한다. 이 결함이 몇 달을 살아남은 이유가 정확히 "표면화되지 않음"이었다. 그리고 **계측 자리를 한 곳에만 두지 않는다**: spec-first 출구만 계측했더니 unowned 차단이 기록 없이 지나가 "한 번도 안 돌았다"로 오회계됐다(실측) — 모든 차단 출구를 계측하고 그 사실을 계약 테스트로 고정한다(SPEC-049).
- **배선 경로는 문자열로 가정하지 않는다 — 그리고 설치 0건은 조용히 넘기지 않는다.** 실측 제보: 도입 프로젝트가 **git worktree** 기반이라 `commit-msg`·`pre-commit`·`pre-push`가 **한 번도 발동한 적이 없었고, 그날의 모든 커밋이 게이트를 우회했다.** 워크트리에서 `.git`은 **파일**이라 `[ -d .git ]` 가드가 실패하고, `--git-dir`은 훅이 없는 워크트리 전용 디렉토리를 준다 — `--git-dir` + `core.hooksPath`를 손으로 합치던 방식이 바로 그 결함의 원인이었다. `git rev-parse --git-path hooks` **한 번**이 worktree·`core.hooksPath`·bare를 동시에 해결한다. 그리고 이 결함을 몇 달간 가린 것은 경로 버그 자체가 아니라 **best-effort 침묵**이었다: 설치기가 "깔았다"고 말한 뒤 그 자리에 실행 가능한 훅이 실제로 있는지 세지 않았다. 이제 설치기가 배선 건수를 실측해 **0건이면 실패로 말한다** — 조용한 0건이 이 결함의 본체다(SPEC-036).
- **결정적인 것과 옳은 것은 다르다 — 틀린 게이트는 틀린 답을 결정적으로 재현한다.** 게이트도 고장 나고, 그 고장은 실패가 아니라 **통과**로 나타나므로 일반 테스트로 드러나지 않는다(실측 제보: 게이트가 19건을 흘리면서 green이었다). 그래서 스윕에 등재된 게이트는 **자기 차단 능력을 증명하는 테스트**를 가져야 한다 — 통과 경로만 관측된 게이트는 clean이 아니라 미검증이다. 이 계약은 배포 폐포 계약과 같은 자리에 두고 **정적으로** 요구한다: 게이트 22종을 픽스처로 실제 실행하는 것이 더 강하지만 그건 훅에서 못 돌 무게이고, 무거운 강제는 우회를 낳는다. 도입 즉시 자기 사각을 잡았다 — 그 라운드에 새로 만든 감시자 게이트 자신이 차단 증명 없이 등재돼 있었다(나머지 21종은 이미 있었다). 그리고 **트리거 소유권**: 감시 대상이 에이전트의 판단인데 감시를 에이전트가 불러야 하면 절차를 건너뛸 때 감시도 같이 건너뛴다 — 의미 판정에 에이전트가 필요하더라도 그 **호출은 게이트가 강제한다**(킷의 확률적 층이 이미 이 구조다: 어댑터를 게이트가 부르고, 그 판정에는 차단력을 주지 않는다).
- **여러 스펙에 걸친 순차 사슬은 아무도 소유하지 않는다 — 그리고 비교는 기록이 만날 자리가 있어야 성립한다.** 실측(사례 5): 8단계 close-out 사슬의 조각이 **6개 문서에 흩어져** 전 구간을 담은 문서가 없었다. 어느 문서를 읽어도 사슬의 일부만 보이므로 세션마다 flow를 재구성하고 매번 다른 곳이 빠졌다. 그리고 그 흩어짐이 **코드의 무행동으로** 나타났다: 교차검증 함수가 `if (!peers.length) return { agree: true }`로 상대 기록이 없으면 통과했고, 양쪽 판정 기록이 만날 저장소가 아예 없었다(로컬은 작업 디렉터리, 클러스터 Job은 볼륨 없는 파드의 `/tmp`). 그 교차검증은 **단 한 번도 비교를 수행한 적이 없고**, 소유자가 결정한 "일치 시에만 전이"는 명세에만 존재했다 — 저장소 요구는 어느 FR에도 없고 코드 주석에만 있었다(인프라 산출물인데 인프라 스펙 밖이라 그쪽 리뷰에서도 빠졌다). 사슬을 스펙들에서 **자동 재구성하지 않는다**: 그러면 게이트가 사슬의 저자가 되고, 재구성이 의도와 다를 때 아무도 그걸 모른다. 사슬은 사람이 한 문서에 쓰고, 게이트는 그 문서가 전 구간을 담았는지와 조각 보유자가 전체를 가리키는지를 본다. 모든 단계에 저장소를 요구하지도 않는다(대부분의 단계는 상태가 필요 없고, 전부 요구하면 사람이 빈 값을 채운다) — **비교·합의 마커와의 곱**으로 좁힌다(SPEC-047).
- **하드코딩을 지양한다 — 어휘·확장자·경로를 게이트에 박으면 그 게이트는 다른 프로젝트에서 조용히 눈을 감는다**(오너 규범). 언어·어휘·문서 종류·파일 배치는 프로젝트마다 다르고, 게이트가 그것을 고정하면 목록 밖 프로젝트에서 판정이 통째로 사라지면서 **그 0건이 진짜 0건과 구분되지 않는다**. 그래서 판정에 쓰이는 값은 전부 `null` 기본(= 킷 기본값 선언)으로 두고 config로 교체 가능하게 하며, 게이트는 **자기가 무엇을 썼는지 출력에 밝힌다**. 실측으로 잡힌 이식성 결함: 소개 문서 게이트가 스윕 규칙표를 `tooling/sdd-sync.mjs`로 고정해 읽었는데 `sdd-init.sh`는 게이트를 `scripts/`에 깔므로, 소비 사이트에서는 게이트 종수 인용이 **조용히 미지원 키**가 되고 문서의 정직한 숫자가 "오타난 키"로 오진됐다. 후보 경로 해석으로 고쳤고(SPEC-036 훅 목록 탐색과 같은 방식), 못 찾으면 **0으로 세지 않고 미지원으로 남긴다** — 모르는 것을 숫자로 말하지 않는다.
- **지목은 배선이 아니다 — 스펙이 이름 댄 메커니즘이 실행 경로에 없으면 그 이름은 장식이다.** 실측(사례 4): FR이 `extractDeployTickets()`를 배포 범위 추출의 메커니즘으로 지목했는데 표면(`Jenkinsfile`)은 그 함수를 부르지 않고 쉘로 같은 일을 다시 구현했다. 두 규칙이 갈라졌고 쉘 쪽에만 있던 결함으로 **19건이 배포 범위에서 조용히 누락**됐다. 게이트는 전부 초록이었다 — 커버 테스트가 단언한 것은 **버그 있는 쉘 구현이 거기 있는지**였고, 지목된 함수는 테스트만 통과하는 고아였다. 여기서 이름의 출처가 결정적이다: **백틱은 추측이 아니라 저술 행위다**(SPEC-042가 거부한 고유명사 자동 추출과 다르다) — 그래서 선언 없이도 오탐이 없고, 한 줄 grep으로 끝난다. 방아쇠 폭도 실측으로 좁혔다: 이름 뒤 공백 불허(`EntityName (relation-type)` 오탐), 테스트 파일명 제외(SPEC-031의 축), bare 식별자 제외(config 키와 구별 불가), **소유 경계를 넘어 탐색**(소유 안에서만 찾으니 정상 모듈 3건이 거짓 고아로 떴다). 참조의 *모양*은 판정하지 않는다 — import 문법을 읽으면 언어에 결합되고, 참조 0회·정의뿐이라는 가장 강한 신호만으로 실측 사례가 잡힌다(SPEC-046).
- **테스트가 FR의 주장이 아니라 구현의 형태를 단언하면, 그 테스트는 회귀를 막는 게 아니라 수정을 막는다.** 같은 실측에서 쉘 구현을 고치자 커버 테스트가 깨졌다 — 그 테스트의 존재 자체가 "구현이 곧 명세"가 되어버린 상태였고, 버그를 지키고 있었다. 기계가 이 상태를 완전히 판정할 수는 없지만(의미 판정은 리뷰의 몫) **값싼 신호는 있다**: FR이 이름 댄 구현체가 그 FR을 커버하는 어떤 파일에도 없다. 그 판정은 SPEC-042의 코어를 **재사용**한다 — 같은 대조를 두 번 구현하면 두 축이 다른 답을 낸다(SPEC-046).
- **설명이 도구보다 늦으면 그 설명은 거짓이 된다 — 소개 문서는 부산물이 아니라 산출물이다.** 규칙표에 R14가 생겼는데 소개 문서는 R13까지였고, 아무 게이트도 그 사실을 몰랐다 — 문서는 어떤 축에서도 판정 대상이 아니었기 때문이다. 새로 배우는 사람은 코드가 아니라 그 문서로 방법론을 만나므로, 낡은 문서는 **존재하지 않는 규칙 체계를 가르치고** 그 오해는 조용히 오래 간다. 그래서 규칙표의 규칙 ID는 소개 문서에 전부 나와야 하고(신설 규칙이 설명 없이 지나가지 못한다), 문서가 `data-sdd-count`로 **자원해서** 올린 숫자는 기계가 검산하고(숫자는 가장 먼저 낡고 가장 늦게 들킨다), 규칙표를 고친 changeset에는 소개 문서가 함께 있어야 한다(코드가 스펙을 동반하는 것과 같은 규율). 문서를 규칙표에서 **생성**하는 길은 기각했다 — 서술·비유·실측 이야기가 본체이고 그건 생성할 수 없다(생성하면 표만 남고 이해가 사라진다). 그리고 **면제 경로를 두지 않는다**: 문서 갱신을 면제로 넘길 수 있으면 그 면제가 곧 정상 경로가 된다(SPEC-045).
- **배포 목록의 드리프트는 규범으로 세 번 실패했다 — 그래서 계약이 됐다.** 게이트를 배포하면서 그 게이트가 import하는 모듈을 빠뜨리면 소비 프로젝트는 게이트가 아니라 `ERR_MODULE_NOT_FOUND`를 받는다. 이 결함은 킷에서 세 번 재발했고(verdict-lib·verification-run-lib·term-coverage-lib) 세 번 다 "다음엔 목록도 같이 고치자"로 끝났다. **규범으로 두 번 이상 실패한 것은 기계가 잡는다** — import 전이 폐포와 규칙표 등재 게이트가 배포 목록에 모두 있는지 계약 테스트가 강제한다(SPEC-004).
- **반복의 기억은 게이트마다 손으로 심지 않는다 — 판정의 공통 종료 지점 하나에 얹는다.** 실측: 에이전트가 하루에 같은 실수를 세 번 했고(R23), 게이트는 세 번 다 정확히 잡았지만 "오늘 세 번째"라는 정보는 어디에도 없었다. 게이트마다 원장 append 코드를 개별로 심으면 반드시 빠뜨리는 게이트가 생기고, 그 게이트가 다음 결함의 자리가 된다 — 이 킷의 손목록·손 파싱이 반복적으로 죽어온 바로 그 형태다. 그래서 R24의 append는 모든 판정 게이트가 이미 거치는 단일 종료 훅(`armVerdict`, SPEC-040)에 얹었고, 그 결과 R23을 포함한 기존 게이트 전부가 **코드 변경 0줄**로 원장에 자동 참여했다. 새 판정 게이트를 추가할 때도 이 참여는 자동이다 — 잊을 자리 자체가 없다(SPEC-057).
- **실패 클래스는 선언이지 추측이 아니다 — 텍스트를 분류하면 그 분류는 이미 추측이다.** R24가 실패를 "같은 종류"로 묶어 세려면 분류축이 필요한데, 그 축을 실패 문장의 정규식·키워드 매칭으로 만들면 분류가 틀릴 때마다 오탐 에스컬레이션이 쌓이고, 오탐이 잦은 축은 꺼진다(오탐 게이트를 만들지 말라는 이 킷의 오래된 규범과 정면으로 만난다). 그래서 클래스는 게이트가 자기 판정 호출에서 `verdict(kind, detail, { class, target })`로 **스스로 선언**할 때만 존재하고, 선언하지 않은 게이트의 실패는 원장에는 남되(가시성) 에스컬레이션 집계에는 들어가지 않는다(강제는 선언한 축에서만) — 이 methodology가 이미 "추측 금지"로 굳힌 원칙을 원장 축에도 그대로 적용한 것뿐이다(SPEC-057).
- **파일이 배포된 것과 그 파일이 요구된 export를 갖는 것은 다른 사실이다 — 절반만 새것인 설치는 게이트가 아니라 크래시를 낸다.** 위 배포 폐포 계약은 *파일이 목록에 있는가*를 보는데, 실측 제보는 한 칸 더 안쪽이었다: 최신화 diff가 공유 lib 27개를 빠뜨려 **게이트는 최신·lib은 구판**인 상태가 됐고 소비처는 판정 대신 `SyntaxError: … does not provide an export named …`를 받았다. **파일은 거기 있었으므로** 폐포 계약을 통과했다. 그래서 import 그래프의 무결성을 별도 축으로 본다 — 이 축만 **아무 게이트도 실행하지 않고** 판정하며(정적으로 결정 가능한 것을 사람에게 맡기지 않는다), 파일 없음·export 없음·**확인 못 함**을 각각 다른 사실로 낸다. 오탐이 이 축의 사망 원인이므로 도입 전에 킷 전체를 교정 집합으로 쟀고(명명 import 456건·오탐 0), 파서가 확정 못 하는 형태는 "없다"고 단정하지 않고 자백한다. 판정 시작점은 게이트 자신의 디렉터리이고 0건이면 INERT다 — **0건은 '깨끗함'이 아니라 '볼 것이 없음'이다**(SPEC-050).
- **크래시의 마지막 줄은 원인이 아니다 — 침묵이 아니라 오진도 결함이다.** 위 결함이 났을 때 스윕이 보고한 사유는 `Node.js v22.22.2`였다. 집계기가 stderr의 **마지막 줄**을 요약으로 뽑았고 크래시 stderr의 마지막 줄은 런타임 배너였으므로, 원인 줄은 스택 프레임 위에 묻혀 제보자가 직접 읽어야 했다. *마지막 줄이 요약인 것은 게이트가 협조적으로 끝났을 때만 참이다 — 크래시는 협조가 아니다.* 이제 스택 프레임·캐럿·배너를 **형태로** 걸러낸 뒤 던져진 오류 줄을 고른다(런타임별 오류 어휘 목록을 유지하는 길은 기각했다 — 목록 밖 런타임에서 통째로 빗나가고 그 실패가 조용하다). 게이트 크래시는 원래도 미판정으로 계상돼 초록이 아니었지만, **사유가 쓸모없으면 사람이 원인까지 가는 길이 없다**(SPEC-050).
- **엔트리 가드를 문자열로 비교하면 게이트가 조용히 실행되지 않는다 — 그리고 이 규범은 복사되면 잊힌다.** `import.meta.url === \`file://${process.argv[1]}\`` 비교는 경로에 비-ASCII가 있으면(`import.meta.url`만 퍼센트 인코딩된다) 또 macOS `/var`↔`/private/var` 심볼릭 링크에서 갈리고, 갈리면 main 블록이 **실행되지 않은 채 exit 0**이다 — 통과가 아니라 무음 미실행이다. 킷은 이 결함을 한 번 고쳤는데 정의가 세 파일에 각자 복사됐고, 그 뒤 새로 만든 게이트 3종(R15·R16·R17)이 **깨진 형태를 다시 도입했다.** 규범이 세 번 복사되면 네 번째 사본은 규범을 모른다 — 정의를 `verdict-lib` 한 곳에 두고 깨진 형태의 재유입을 계약 테스트가 금지한다(SPEC-040). 고친 것으로 끝내지 않는다.
- **감시자가 있다와 감시자가 에이전트를 본다는 다른 사실이다.** 오너 실측: "SDD에 의해 수행하는지 혼자 날뛰지 않는지 봐야 하는데 **그게 동작을 하지 않아.**" 조사 결과 원인 다섯이 전부 **조용한 0건** 계열이었다. 가장 결정적인 것: **킷 자신에 `.claude/`가 없었다** — 킷은 모든 축을 자기적용하는데 이 층만 도그푸딩이 0이라, 이 배선이 동작하는 것을 한 번도 관측한 적이 없었다. 그런데도 감시자 축(R17)은 초록이었다: R17은 **커밋 이후** 채널(CI)과 채택 영수증을 보고, 에이전트 훅은 **아무 축의 판정 대상이 아니었다.** git 훅도 커밋 시점에 이미 작성된 코드를 본다 — "지금 스펙 없이 코드를 쓰고 있다"를 그 자리에서 말할 수 있는 층은 에이전트측 훅뿐이다. 나머지 넷도 같은 계열이었다: 영수증이 git 훅만 기록했고, `jq` 미설치가 "배선 스킵 + 설치 성공"이었고(워크트리 결함을 가린 침묵과 같은 모양), 편집 가드의 코드 경로가 `src/|lib/|app/`로 하드코딩돼 킷의 `scanDirs`(`tooling`)에서는 **발화할 수 없었다** — 주석은 "설치기가 조정한다"고 적혀 있었지만 설치기는 그 파일을 복사만 했다. 이제 훅 목록은 **단일 선언**이고(설치기와 게이트가 같은 파일을 읽는다 — R12에서 배운 것), 배선은 그 선언에서 계산되며, 실패는 큰 소리로 멈추고 배선 직후 자기검증한다. **자동 치유는 두지 않았다**: 판정 게이트가 판정 대상을 고치면 언제나 초록이고 초록의 의미가 사라진다(SPEC-051).
- **훅이 있어도 낡으면 없는 것과 같다 — 그리고 설치기가 "있으면 skip"하면 낡음이 영구화된다.** 실측 제보(소비 프로젝트): 훅 배선 게이트가 **낡은 사본을 green으로 보고했고** hard로 켜둔 감시 게이트가 한 번도 발동하지 못했다 — 저장소본(31행)에는 게이트 호출이 있는데 설치본(26행)에는 없었고, 누락된 5행이 **호출 블록 전체**였다. 게이트를 직접 부르면 exit 1·위반 3건이었으니 게이트는 옳았고 배선이 낡았을 뿐이다. 이것은 이 축이 막으려던 실패의 **재발**이다: 전제가 "훅이 없으면 아무것도 발동하지 않는다"였는데 **훅이 있어도 낡으면 같은 결과**라는 경우가 판정에서 빠져 있었다(훅 경로를 문자열로 가정해 워크트리에서 조용히 skip한 선례와 같은 층). 그래서 판정 종류에 `stale`(미설치와 동급)과 `source-unreadable`(확인 못 함 — 통과 아님)을 더했고, 설치기는 **킷 소유 사본을 내용이 다르면 항상 갱신**한다. 단 `sdd.config.json`·템플릿처럼 **프로젝트가 편집하는 씨앗 파일은 반대로 보존**한다 — 의미가 다른 두 복사를 한 함수에 담지 않는다(SPEC-036).
- **면제는 "지금 green을 만들기 위해" 추가되고 아무도 걷어내지 않는다 — 그래서 면제도 래칫된다.** 강도·임계 래칫이 knob을 지키는 동안 **면제 목록이 게이트를 무력화하는 우회로**였다(실측 제보: 어떤 저장소의 게이트 다수가 면제로 꺼져 있었다). 이 축의 직접 근거는 제보자의 자기관찰이다 — 새 게이트를 세우자 결손이 표면화됐고 그의 반사적 선택지가 "면제해서 green 만들기"였으며 오너가 막았다("왜 필요할 때는 제외시키면 안 되지"). **게이트를 세우는 순간이 면제 유혹이 가장 큰 시점이다.** 그래서 면제 항목마다 `exemptionRegistry`에 분류와 사유를 요구하고, 개수는 **줄어드는 방향만** 허용한다. ⚠ 면제를 한 종류로 다루지 않는다: `boundary`(구조적·영구)에 기한을 요구하면 **거짓 날짜**가 생기고 거짓 날짜는 날짜 없음보다 나쁘다 — 대신 "왜 영구인가"를 요구한다. `debt`는 사유·해소조건·기한·위험수용자 4종 전부다. 종류 미선언은 위반이다(분류를 강제해야 debt를 boundary로 위장한 흔적이 남는다). 면제 knob은 **이름 규약으로 자동 탐지**한다 — 손 목록은 새 knob을 놓친다(강도 래칫이 이미 겪었다). 예외 선언 knob 자신은 개수 래칫에서 빼되 그 항목도 4필드를 요구받는다 — 교착의 해소는 캡을 푸는 것이 아니라 **출구를 만드는 것**이다(SPEC-027).
- **감시와 감사는 다른 층이고, 감사자는 감시 대상이 될 수 없다.** 오너가 구조를 셋으로 정리했다: **감시 게이트**(저장소 상태가 명세와 맞는가 — 커밋·푸시·CI) · **감시 에이전트**(대화 세션 에이전트가 *지금* 명세대로 하는가 — 도구 호출 순간) · **감사**(명세 코퍼스가 스스로와 정합한가). 감사를 감시 에이전트에 겸하게 하지 않은 이유는 순환이다 — 급할 때 명세를 무시하는 그 에이전트에게 "명세 충돌도 같이 봐 줘"를 맡기면 그 점검이 **가장 먼저 생략된다**(고발 장치가 고발 대상의 협조를 요구하면 그것은 강제가 아니다). 그리고 감사도 **둘로 쪼갠다**: 같은 대상에 `SHALL` ↔ `SHALL NOT`은 정적으로 결정되므로 게이트가 차단하고(R20), "동일 기능에 1은 A, 2는 B"는 확률적이라 차단력을 주지 않고 **쌍을 기계가 전수 열거하고 사람·LLM이 판정**한다(R10 ③층과 같은 분업 — 전수성은 열거기가 보장한다). 하나로 두면 결정적으로 잡을 수 있는 모순이 "권고"로 내려앉는다. ⚠ 이 축의 오탐 억제가 설계의 절반이다: 도입 전 킷 코퍼스(지시 441건)로 재며 세 번 조정했고, 마지막 조정은 흔한 술어를 **어휘 목록이 아니라 말뭉치 희귀도**로 가른 것이다(목록은 프로젝트마다 다르고 목록 밖에서 오탐이 되살아나는데, 통계는 그 코퍼스에서 스스로 계산된다). 그리고 게이트는 **어느 쪽이 정본인지 정하지 않는다** — 추정으로 고른 정본이 다음 모순의 씨앗이 된다(SPEC-052).
- **조회는 커밋을 남기지 않는다 — 그래서 "명세를 읽었는가"는 도구 호출 직전에만 강제할 수 있다.** 실측: 에이전트가 배포 실패 원인을 조사하며 명세에 **이미 적혀 있는** 문자열을 실측으로 다시 찾아 원인으로 단정했고, 같은 문서에 소유자 결정이 기록돼 있었는데도 **결론까지 틀렸다**(진짜 원인은 다른 층이었다). 오너가 여러 세션에 걸쳐 금지한 경로가 재발했다. **읽기가 재량이면 급할 때 가장 먼저 생략된다.** 그런데 커밋 게이트는 이 행동을 원리상 볼 수 없다 — 조회는 커밋도 파일 변경도 남기지 않는다. 결정적인 발화 지점은 **도구 호출 직전** 하나뿐이고, 거기서 명령 패턴 → 관련 명세 매핑을 띄우는 것은 결정적이다. 두 강도를 둔다: `surface`(막지 않고 **절 위치까지** 띄운다 — 이름만 주면 처음부터 읽어야 하고 급할 때 처음부터 읽는 사람은 없다) / `deny`(막는다, 단 **대신 볼 곳을 반드시** 준다 — 막기만 하면 사람은 아무도 모르는 우회로를 찾는다). ⚠ **명세를 읽는 명령은 모든 규칙을 이긴다**: 읽기를 막으면 "읽어라"면서 읽기를 막는 자기모순이고 오탐이 즉시 쌓인다. 그리고 금지 목록을 게이트에 하드코딩하지 않는다 — 선언으로 빼고 **선언 자체를 판정**한다(이 축의 자기결함은 조용한 무발화다: 깨진 정규식은 아무것도 막지 않고 아무것도 알리지 않는다). 이 가드는 감시 에이전트 층에 살고 배선 실재는 R19가 판정하며, R20(코퍼스 정합)과 **짝**이다 — 052 없이 053만 있으면 모순된 명세를 성실히 읽게 만드는 셈이다(SPEC-053).
- **검사 못 함을 통과로도 위반으로도 말하지 않는다 — 그리고 그 통로는 게이트가 아니라 코어에 있어야 한다.** SPEC-040은 **게이트**가 자기 판정 종류를 선언하게 만들지만, 그 선언은 게이트가 **코어 반환값을 해석한 결과**다. 코어에 "못 봤다"의 통로가 없으면 게이트는 그 사실을 알 방법이 없고 빈 결과를 clean으로 읽는다 — **판정이 사라지는 자리는 코어와 게이트의 경계다.** 실측이 양쪽 방향을 다 보여줬다: 훅 사본 판정기가 "원본을 못 읽음"이라는 경우를 아예 갖지 않아 **낡은 훅이 green**이 됐고(강제 정책이 한 번도 발화하지 않았다), 반대로 존재 판정기 4종이 `boolean`만 받아 읽기 실패가 `false`로 붕괴해 **"파일 없음"이라는 거짓 위반**이 가능했다. 그래서 종류를 셋으로 못박고(봤고 문제 없음 / **검사 못 함** / 위반) 판정 코어의 **반환 형태**로 만들었다(SPEC-054). 검사 못 함은 **차단하지 않고 초록에도 합산하지 않는다** — 못 본 것으로 빌드를 깨면 사람이 그 층을 떼어내고, 통과로 세면 그것이 제보가 겪은 그 결함이다. 넷째 종류는 만들지 않는다(경계 판단이 필요한 분류는 급할 때 편한 쪽으로 기운다). 계약 모듈은 **아무것도 import하지 않는다** — 무거운 계약은 채택되지 않고, 채택되지 않은 계약은 계약이 아니다. 그리고 규범으로 적지 않고 **존재 판정기를 주입받는 코어를 소스에서 전수 열거해** 통로 유무를 검산한다: 한 코어라도 통로가 없으면 그 코어가 다음 결함의 자리다.
- **경로를 git에게 물을 때 인용을 끄지 않으면 판정이 양방향으로 틀린다 — 그리고 이 계약은 전수여야 한다.** `git diff --name-only`는 비ASCII 경로를 8진수 문자열로 **인용해서** 낸다(`"docs/\353\260\251\353\262\225\353\241\240.html"`). 그 문자열은 어떤 소유 글롭·문서 목록과도 매치하지 않으므로 **소유 귀속이 조용히 사라지고**(위반이 통과로), 동시에 **자기 갱신을 놓친 것으로 오판한다**(통과가 위반으로). 도그푸딩 실측: 소개 문서 게이트가 **자기 문서를 고친 커밋을 "문서 그대로"라고 차단했다.** 두 게이트는 이미 `sh` 래퍼로 정규화하고 있었고 **여덟 곳이 빠져 있었다** — 규범으로 적혀 있었기 때문이다. 그래서 `core.quotepath=off`를 두 런타임 소스에서 **전수 열거해** 검산한다(`runtime-contract.test.mjs` ④). 열거기는 **프로세스를 띄우는 줄만** 본다: 래퍼 호출부는 기동이 아니고 오류 문구 속 `git diff(...)`는 산문이다 — 이 구분을 놓친 첫 판이 산문 2줄과 래퍼 호출 3줄을 잡았고, **오탐이 잦은 게이트는 꺼진다.** 이것은 `isMainEntry`의 퍼센트 인코딩 결함과 같은 계열이다: **경로를 문자열로 가정하는 순간 비ASCII 저장소에서 게이트가 눈을 감는다.**
- **마커가 라벨에 걸린 것을 호출로 읽으면, 감시자가 자기 이름에 속는다 — 그리고 CI의 게이트 손목록은 반드시 뒤처진다.** 실측(2026-08-10, 킷 자기적용): R17의 스윕 마커 목록에 `sdd-gates`가 있고 킷 워크플로가 `sdd-gates.yml`(안에 `name: sdd-gates`)여서 **감시자 실재를 판정하는 게이트가 자기 파일명에 매치해** 여러 달 "✓ 우회 불가한 채널에 스윕이 배선돼 있다"를 보고했다. CI는 스윕을 **한 번도 부르지 않았고**, 그 사이 스윕 등재 25종 중 **10종이 어떤 우회 불가 층에도 없었다** — R13·R15·R16·R18·R19·R20·R21·R5·R12, 그리고 **그 게이트 자신.** 즉 감시·감사 층 대부분이 사람이 손으로 스윕을 칠 때만 돌았다. 선행 문맥(러너·경로)만 고치면 두 번째 거짓 양성이 남는다: 인자 안의 `.yml` 경로가 `/` 선행으로 매치한다 — **데이터 파일은 프로그램이 아니다.** 그리고 "아예 없음"과 "라벨에만 있음"은 **다른 위반**이다: 없으면 게이트가 고발하지만 라벨에 걸리면 아무도 고발하지 않는다. `--strict` 없는 스윕 호출도 마찬가지다 — **보고하고 통과하는 채널은 채널이 아니라 로그다.** 해소는 CI 목록을 고치는 것이 아니라 **없애는 것**이다: 스윕이 게이트 목록의 정본이므로 CI는 스윕 한 줄을 부르고, 게이트를 추가해도 그 파일은 바뀌지 않는다 — **바뀌지 않는 것이 요점이다.** 이전 판은 손목록 위에 "게이트를 추가하면 여기에도 추가한다"는 주석을 달았고 그 규범은 실패했다(설치기 복사 목록·테스트 픽스처 목록이 이미 같은 방식으로 실패했다 — **목록은 적는 것이 아니라 계산하는 것이다**).
- **git은 훅을 복제하지 않는다 — 그래서 CI에서 훅 부재는 미채택의 증거가 아니다.** 스윕을 CI에 배선하는 순간(그것이 유일한 우회 불가 채널이다) 훅 배선 축은 CI에서 **언제나 거짓 위반**을 내게 되고, `hard` 프로젝트의 빌드가 거짓으로 깨지면 그 다음은 정해져 있다 — 사람이 게이트를 끈다. 그래서 그 환경에서는 **SKIPPED로 선언한다**(사유 있는 생략 — SPEC-040). 완화가 아니다: 관측 불가한 사실을 위반이라 말하지 않는 것이고, 강제도 사라지지 않는다 — CI에서 채택의 관측 가능한 대리물은 **채택 영수증**이고 그것은 R17이 판정한다. 이 축은 **로컬 채택 축**으로 정직하게 남는다.
- **선언되지 않은 대응은 대조되지 않는다 — 양판 의무도 규범으로 적히면 새어나간다.** SPEC-006은 판정 게이트에 양판을 요구하는데, **어느 게이트가 어느 런타임 서브커맨드에 대응하는지가 어디에도 없었다** — 그래서 미러 누락은 사람이 손으로 대조할 때만 발견됐다. 실측(2026-08-10): 그 대조를 선언(`PY_SUBCOMMAND`)+소스 열거로 기계화한 **첫 실행이 즉시 2건**을 냈다 — R12 훅 배선과 R13 구현 중복이 여러 라운드 동안 Node 전용이었고, Python 런타임 프로젝트에서 그 두 축은 **아무도 보지 않는 상태**였다(그 `0건`은 진짜 0건과 구분되지 않는다). 제외 항목에는 **사유를 요구한다**: 빈 값은 "판정 게이트가 아니다"와 "잊었다"를 구분하지 못하고, 구분되지 않는 빈칸은 항상 후자를 숨긴다. 선언은 **양방향**으로 본다 — 스윕에서 사라진 게이트가 선언에 남아 있으면 그 줄은 죽은 선언이고, 죽은 줄이 쌓이면 선언 전체를 아무도 믿지 않는다.
- **파생 신호는 완료의 근거가 아니다 — 로그와 CI 상태는 대상이 아니라 대상에 대한 이야기다.** 실측 제보: 파이프라인 로그에 성공 줄이 있고 CI가 초록이어서 **배포 완료로 보고했는데 migrate Job 실패로 배포 스테이지가 스킵된 상태**였다. 파생 신호는 대상이 정상일 때도 정상이고 **대상이 실패했을 때도 정상일 수 있다** — 스킵되면 실패 줄조차 남지 않는다. 그래서 완료를 주장하는 검사는 무엇을 관측했는지 선언하고(`target-state`·`derived`·`self-report`, 순서가 곧 등급이다) 하한은 `target-state`다(SPEC-055). **증거 자산의 등급(SPEC-031)과 직교한다**: 브라우저 등급 자산이라도 그 실행 로그를 읽어 완료를 말하면 파생 신호이고, 그 조합이 이 사고의 형태였다. 명령을 정적 분석하지 않는 이유 — 어휘 목록으로 판정하면 목록 밖에서 오탐·미탐이 살아나고 킷이 그 프로젝트의 인프라를 안다고 전제하게 된다. 미선언에 기본값을 주지 않는 이유 — **추정한 기본값은 조용히 정답이 되고**, 그 순간 선언을 요구하는 축이 아무것도 요구하지 않는 축이 된다. 해소 안내를 "선언을 고쳐 적어라"가 아니라 **"대상을 조회하는 검사를 더해라"**로 쓴 이유 — 명령을 그대로 두고 선언만 바꾸면 그 선언이 거짓이 되고, 게이트가 값싼 해소를 안내하면 사람은 그것을 택한다.
- **설치기 안 heredoc은 정본이 될 수 없다 — 대조할 원본이 없으면 신선도는 영구히 미판정이다.** 킷은 낡은 훅 사본을 잡는 축을 세우면서 **자기 훅은 설치기 안 heredoc으로 만들었다** — 그래서 그 축이 킷 자신에게는 한 번도 발화하지 못했다(SPEC-051에서 겪은 것과 같은 계열: 킷에 `.claude/`가 없어 에이전트 배선 축만 도그푸딩이 0이었다). 훅 본문을 파일로 빼고 목록이 그 경로를 선언하면 낡은 사본이 즉시 `stale`로 잡힌다. 그리고 **`source` 열은 설치 방식에 종속이다**: 킷의 `.git/hooks/<이름>`은 사본이라 바이트 대조가 성립하지만 소비처의 그것은 스크립트를 부르는 **생성된 래퍼**라 사본 관계가 없다 — 한 목록에 적으면 소비처에서 있지도 않은 원본을 "읽지 못했다"고 보고한다. **거짓 위반은 오탐이고, 오탐이 잦은 게이트는 꺼진다** — 그래서 킷 전용 목록을 따로 두고 배포 목록에서 뺀다.
- **추측이 금지인 방법론에서 추측의 성공은 침묵할 수 없다 — 그리고 강도 사다리의 종착지는 도달 가능해야 한다.** 킷은 역할 선언이 없으면 카테고리 **이름 정규식**으로 폴백하고(SPEC-001 FR-010), 그것도 실패하면 입도 축은 **첫 카테고리**를 집었다. 두 폴백 모두 **실패할 때만** 사유를 남겼다 — 성공하면 판정이 추측 위에서 진행되고 아무도 모른다(개명 한 번에 조준 대상이 조용히 바뀐다). 그래서 판정 입력의 **출처**(선언·이름 추론·미해석)를 판정과 **같은 규칙으로** 계산해 매 실행 자백한다. 출처를 다른 규칙으로 계산하면 그 보고가 거짓이 된다. 같은 라운드의 짝: 편집 시점 spec-first는 `off|advisory`에서 끝나 **금지할 수단이 아예 없었고** 쉘 래퍼는 종료코드를 버렸다 (`2>/dev/null` + 무조건 `exit 0`) — **종착지가 도달 불가한 사다리는 사다리가 아니다.** 차단을 추가하면서 세 가지를 함께 지켰다: ①차단해도 체크리스트를 먼저 보여준다 ②판정 못 하는 자리(git 없음·미소유·변경 집합 미해석)는 절대 막지 않는다 ③`hard`인데 판정할 런타임이 없으면 **거짓 안전을 자백한다**. 그리고 래칫 전수성 테스트가 **킷이 선언한** knob만 보고 있었다 — 강도 knob 40종 중 18종이 미선언이라 사정거리 밖이었고 그 사각에 감시 없는 knob 1종이 살아 있었다: **열거기가 잘못된 집합을 보면 그 전수성은 전수성이 아니다.**
- **재검토 조건은 방아쇠를 빠뜨릴 수 있다 — 위험이 실현되면 결정을 몰래 뒤집지 말고 조건을 갱신하고 그 처방을 이행한다.** 실측: "Python 미러 없음(의도)" 결정의 재검토 조건이 **"스윕 규칙으로 승격되면"** 하나였는데, 실제로 터진 방아쇠는 **차단 강도 획득**이었다 — 조건을 적을 때 상상한 경로만 적으면 다른 경로로 온 위험은 조건을 그대로 통과한다. 차단할 수 있는 층이 한 런타임에만 있으면 다른 런타임 프로젝트는 `hard`를 켜고도 보호가 0이다(**hard 선언 + 무판정 = 거짓 안전**). 그래서 조건에 그 방아쇠를 추가하고 곧바로 복제했다. 그리고 **미러에는 호출부가 있어야 한다** — 원래 결정의 근거가 "미러가 있어도 호출부가 없다"였고, 훅 쉘이 런타임을 고르지 않으면 복제한 판정은 **죽은 코드**이며 커버리지 숫자만 올린다. 같은 라운드에 방아쇠가 터지지 않은 결정(비차단 배포 훅 헬퍼 3종의 Node 전용)은 **그대로 두었다** — 조건이 살아 있는 결정을 함께 뒤집는 것은 근거 없는 확대다.
- **커밋 게이트는 대화 안에서 끝나는 사고를 원리상 볼 수 없다 — 그 자리를 보는 층은 도구 호출 직전뿐이다.** 실측 제보: QA 실측·트래커 댓글까지 마친 티켓을 아직 배포도 안 된 채 종결 상태로 넘기려 했다. 재현도 커밋도 없이 대화 안에서 끝났으므로 `check-spec-sync` 같은 commit-msg 게이트는 관여할 지점이 없다 — SPEC-053(진단가드)·SPEC-035(배포가드)가 이미 서 있는 같은 층(PreToolUse)에 R25가 합류하는 이유다. 다만 구제 방식은 다르다: 정적 지목("이미 답이 있는 명세를 읽어라")·정적 전제 조건("커밋되지 않은 트리다")과 달리, 되돌리기 어려운 행동(트래커 전이·배포·파괴적 DB 조작)에는 **동적 증거**(독립 검증이 실제로 일어났다는 마커)를 요구한다 — 그래서 세 축을 하나로 합치지 않고 SPEC-058로 따로 세웠다(SPEC-058).
- **승인 마커는 세션도 패턴도 아니라 정확한 행동 해시에 결속한다 — 결속 범위를 넓히면 승인의 대상 특정성이 사라진다.** "한 번 확인했으니 이 종류 행동은 당분간 다 통과"로 완화하면, 티켓 A의 종결 승인이 티켓 B의 종결까지 덮는 사고가 그대로 재현된다 — 마커가 막으려는 바로 그 문제를 마커 자신이 재도입하는 꼴이다. 그래서 승인은 실행될 명령 문자열의 sha256 해시에 결속되고, 한 글자라도 다른 명령은 다른 해시라 별도 승인이 필요하다. 신선도(TTL)만 프로젝트가 조정할 수 있고, 대상 특정성은 완화 대상이 아니다(R25).
- **게이트는 서브에이전트를 스스로 부르지 않는다 — 판정 게이트는 결정론적이어야 두 런타임이 같은 답을 낸다.** R25가 요구하는 것은 "독립 검증이 일어났다는 증거"이지 그 검증 자체가 아니다. 검증을 게이트 프로세스 안에서 LLM을 불러 수행하면 그 판정은 더 이상 결정론적이지 않고, Node·Python 양판(SPEC-006)이 성립하지 않는다. 그래서 차단 메시지는 "별도 컨텍스트의 서브에이전트로 확인하고 `--record`로 마커를 남겨라"는 **지시**로 끝나고, 실제 호출은 차단당한 실행기 자신이 자기 도구로 한다 — 완전 자율 감사 에이전트(SPEC-057에서 이미 범위 밖으로 그은 것과 같은 경계)를 만들지 않으면서도, 검증의 존재는 결정론적으로 강제한다.
- 게이트는 런타임 중립(4판), 실행기만 에이전트별.
- 로컬 훅·TDD는 `commands.test`(로컬 안전 유닛)만, 인프라 테스트는 개발서버·CI에서 `commands.smoke` — 로컬은 인프라 의존 테스트를 강제하지 않는다. 정본: METHODOLOGY §"검증은 환경으로 계층화된다"(+ `sdd.config.presets` 테스트 tier).

## 완료 루프의 꼬리 — 원점 트래커 close-out
작업이 tracked issue(QA/이슈 트래커)에서 유래했다면 verify/merge가 끝이 아니다 — **①트래커 dev-done(개발자) → ②이해관계자 완료 보고(무엇·왜·어떻게+검증 경로) → ③리포터 confirm(리포터/QA)**까지가 완료다. **2인 책임분리**: 개발자는 리포터의 confirm을 건드리지 않는다. 외부 시스템·사람 sign-off라 게이트가 아니라 규범이며(SC 충족과 동일 — 리뷰 경계), 실행기는 `speckit-fix` 스킬 마지막 단계(§완료형 스킬)다. 트래커 정체·보고 채널(수신자·형식)은 킷에 하드코딩하지 않고 `trackerCloseout` config(또는 CLAUDE.md 관례)로 인스턴스화한다(`{}`=비활성).
